version: 1

filesystem_policy:
  include_workdir: false
  read_only:
    - /usr
    - /lib64
    - /etc
    - /proc
    - /dev/urandom
    - /opt/openshell-lab
  read_write:
    - /tmp
    - /dev/null

landlock:
  compatibility: hard_requirement

process:
  run_as_user: "1500"
  run_as_group: "1500"

network_policies: {}
